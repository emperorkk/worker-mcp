{
  "name": "softone-mcp-worker",
  "version": "0.1.1",
  "description": "Cloudflare Worker remote MCP server for SoftOne Web Services",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --config wrangler.toml",
    "deploy:ci": "wrangler deploy --config wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.71.0"
  }
}
